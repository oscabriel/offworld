import { describe, it } from "vitest";

/**
 * Unit tests for Convex reference functions
 * TODO: Update tests after US-010 migration
 */

describe.skip("references", () => {
	it("placeholder", () => {
		// Tests need update for reference model
	});
});
