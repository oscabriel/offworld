# Environment files are managed separately:
# - .env.dev: Development Convex URLs
# - .env.prod: Production Convex URLs
#
# These are used by Alchemy via --env-file flag in package.json scripts

VITE_CONVEX_URL=
VITE_CONVEX_SITE_URL=