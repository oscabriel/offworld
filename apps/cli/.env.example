# Required for CLI authentication and remote sync
# Copy this file to .env and fill in your values

# WorkOS OAuth Client ID (for `ow auth login`)
WORKOS_CLIENT_ID="your_workos_client_id_here"

# Convex deployment URL (for `ow pull`, `ow push`, etc.)
CONVEX_URL="https://your-deployment.convex.cloud"

# NOTE: These env vars are:
# - Injected at BUILD TIME for production/published builds
# - Loaded at RUNTIME from this .env file for local development
# 
# For local development with `bun link`:
# 1. Copy this file to .env
# 2. Fill in your values above
# 3. Run `bun run build` in this directory
# 4. Run `bun link` to make the `ow` command available globally
# 5. The CLI will load these values from .env at runtime
